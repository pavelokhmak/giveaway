# Meta / Instagram Graph API credentials (server-side only — never exposed
# to the client). If these are left empty, the app automatically falls
# back to Demo Mode and works without any Instagram API access.
INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=
